name: misty-server-production

x-api-image: &api-image
  image: ${MISTY_API_IMAGE:?set immutable MISTY_API_IMAGE}

x-activepieces-environment: &activepieces-environment
  AP_EDITION: ce
  AP_ENVIRONMENT: prod
  AP_FRONTEND_URL: ${ACTIVEPIECES_PUBLIC_URL:?set ACTIVEPIECES_PUBLIC_URL}
  AP_ENCRYPTION_KEY: ${ACTIVEPIECES_ENCRYPTION_KEY:?set ACTIVEPIECES_ENCRYPTION_KEY}
  AP_JWT_SECRET: ${ACTIVEPIECES_JWT_SECRET:?set ACTIVEPIECES_JWT_SECRET}
  AP_DB_TYPE: POSTGRES
  AP_POSTGRES_HOST: activepieces-postgres
  AP_POSTGRES_PORT: "5432"
  AP_POSTGRES_DATABASE: ${ACTIVEPIECES_POSTGRES_DATABASE:?set ACTIVEPIECES_POSTGRES_DATABASE}
  AP_POSTGRES_USERNAME: ${ACTIVEPIECES_POSTGRES_USERNAME:?set ACTIVEPIECES_POSTGRES_USERNAME}
  AP_POSTGRES_PASSWORD: ${ACTIVEPIECES_POSTGRES_PASSWORD:?set ACTIVEPIECES_POSTGRES_PASSWORD}
  AP_REDIS_TYPE: STANDALONE
  AP_REDIS_HOST: activepieces-redis
  AP_REDIS_PORT: "6379"
  AP_REDIS_USER: default
  AP_REDIS_PASSWORD: ${ACTIVEPIECES_REDIS_PASSWORD:?set ACTIVEPIECES_REDIS_PASSWORD}
  AP_EXECUTION_MODE: SANDBOX_CODE_ONLY
  AP_WORKER_CONCURRENCY: "1"
  AP_FILE_STORAGE_LOCATION: DB
  AP_QUEUE_UI_ENABLED: "false"
  AP_TELEMETRY_ENABLED: "false"
  AP_TOOL_SEARCH_ENABLED: "false"

services:
  postgres:
    image: pgvector/pgvector:pg16@sha256:1d533553fefe4f12e5d80c7b80622ba0c382abb5758856f52983d8789179f0fb
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_MIGRATION_USER:?set DB_MIGRATION_USER}
      POSTGRES_PASSWORD: ${DB_MIGRATION_PASSWORD:?set DB_MIGRATION_PASSWORD}
      POSTGRES_DB: ${DB_NAME:?set DB_NAME}
      MISTY_APP_DB_USER: ${DB_USER:?set DB_USER}
      MISTY_APP_DB_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./scripts/docker/postgres-init-app-role.sh:/docker-entrypoint-initdb.d/10-misty-app-role.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 3s
      timeout: 5s
      retries: 20
    networks: [database]

  activepieces-postgres:
    image: pgvector/pgvector:0.8.0-pg14@sha256:c55d7e7deac05dde62139e0ded4fcf4f58363656cbc382dbea82fbed995aa767
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${ACTIVEPIECES_POSTGRES_DATABASE:?set ACTIVEPIECES_POSTGRES_DATABASE}
      POSTGRES_USER: ${ACTIVEPIECES_POSTGRES_USERNAME:?set ACTIVEPIECES_POSTGRES_USERNAME}
      POSTGRES_PASSWORD: ${ACTIVEPIECES_POSTGRES_PASSWORD:?set ACTIVEPIECES_POSTGRES_PASSWORD}
    volumes:
      - activepieces_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 30
    networks: [activepieces-database]

  activepieces-redis:
    image: redis:7.0.7@sha256:bb474c35022ca2c5618f4c49ca759bd2c0eea1daf5d934c560bd30092b97b498
    restart: unless-stopped
    environment:
      ACTIVEPIECES_REDIS_PASSWORD: ${ACTIVEPIECES_REDIS_PASSWORD:?set ACTIVEPIECES_REDIS_PASSWORD}
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${ACTIVEPIECES_REDIS_PASSWORD:?set ACTIVEPIECES_REDIS_PASSWORD}"]
    volumes:
      - activepieces_redisdata:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a $$ACTIVEPIECES_REDIS_PASSWORD ping | grep -q PONG"]
      interval: 5s
      timeout: 5s
      retries: 30
    networks: [activepieces-database]

  activepieces-app:
    image: ghcr.io/activepieces/activepieces:0.88.3@sha256:3080dd4c59b5e969589562c1f7d5913b71184790586d0c43b8ea6f8b723b2ddb
    restart: unless-stopped
    environment:
      <<: *activepieces-environment
      AP_CONTAINER_TYPE: APP
    volumes:
      - activepieces_cache:/usr/src/app/cache
    ports:
      - "127.0.0.1:${ACTIVEPIECES_HOST_PORT:-8090}:80"
    depends_on:
      activepieces-postgres:
        condition: service_healthy
      activepieces-redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:80/api/v1/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 30
    networks: [edge, activepieces-database]

  activepieces-worker:
    image: ghcr.io/activepieces/activepieces:0.88.3@sha256:3080dd4c59b5e969589562c1f7d5913b71184790586d0c43b8ea6f8b723b2ddb
    restart: unless-stopped
    environment:
      AP_CONTAINER_TYPE: WORKER
      AP_FRONTEND_URL: http://activepieces-app:80
      AP_JWT_SECRET: ${ACTIVEPIECES_JWT_SECRET:?set ACTIVEPIECES_JWT_SECRET}
      AP_WORKER_CONCURRENCY: "1"
      AP_REUSE_SANDBOX: "true"
      AP_EXECUTION_MODE: SANDBOX_CODE_ONLY
      AP_TELEMETRY_ENABLED: "false"
    volumes:
      - activepieces_cache:/usr/src/app/cache
    depends_on:
      activepieces-app:
        condition: service_healthy
    networks: [edge]

  migrate:
    <<: *api-image
    restart: "no"
    environment:
      GOOSE_DRIVER: postgres
      GOOSE_DBSTRING: >-
        host=postgres port=5432
        user=${DB_MIGRATION_USER:?set DB_MIGRATION_USER}
        password=${DB_MIGRATION_PASSWORD:?set DB_MIGRATION_PASSWORD}
        dbname=${DB_NAME:?set DB_NAME}
        sslmode=disable
      GOOSE_MIGRATION_DIR: /app/migrations
    depends_on:
      postgres:
        condition: service_healthy
    entrypoint: ["/usr/local/bin/goose"]
    command: ["up"]
    networks: [database]

  database-permissions:
    image: pgvector/pgvector:pg16@sha256:1d533553fefe4f12e5d80c7b80622ba0c382abb5758856f52983d8789179f0fb
    restart: "no"
    environment:
      PGHOST: postgres
      PGPORT: "5432"
      PGDATABASE: ${DB_NAME:?set DB_NAME}
      PGUSER: ${DB_MIGRATION_USER:?set DB_MIGRATION_USER}
      PGPASSWORD: ${DB_MIGRATION_PASSWORD:?set DB_MIGRATION_PASSWORD}
      MISTY_APP_DB_USER: ${DB_USER:?set DB_USER}
      MISTY_APP_DB_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD}
    volumes:
      - ./scripts/docker/postgres-grant-app-role.sh:/usr/local/bin/misty-grant-app-role:ro
    depends_on:
      migrate:
        condition: service_completed_successfully
    entrypoint: ["/bin/sh", "/usr/local/bin/misty-grant-app-role"]
    networks: [database]

  api:
    <<: *api-image
    restart: unless-stopped
    env_file:
      - path: .env/prod/runtime.env
        required: true
      - path: .env/prod/database.env
        required: true
      - path: .env/prod/storage.env
        required: true
      - path: .env/prod/observability.env
        required: true
      - path: .env/prod/integrations/ai.env
        required: true
      - path: .env/prod/integrations/discord.env
        required: true
      - path: .env/prod/integrations/dropbox.env
        required: true
      - path: .env/prod/integrations/email.env
        required: true
      - path: .env/prod/integrations/figma.env
        required: true
      - path: .env/prod/integrations/github.env
        required: true
      - path: .env/prod/integrations/instagram.env
        required: true
      - path: .env/prod/integrations/google.env
        required: true
      - path: .env/prod/integrations/microsoft.env
        required: true
      - path: .env/prod/integrations/notion.env
        required: true
      - path: .env/prod/integrations/slack.env
        required: true
      - path: .env/prod/integrations/stripe.env
        required: true
      - path: .env/prod/crypto/documents.env
        required: true
      - path: .env/prod/crypto/journal.env
        required: true
      - path: .env/prod/crypto/devices.env
        required: true
      - path: .env/prod/crypto/spaces.env
        required: true
      - path: .env/prod/crypto/services.env
        required: true
    environment:
      PORT: "8080"
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: ${DB_NAME:?set DB_NAME}
      DB_USER: ${DB_USER:?set DB_USER}
      DB_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD}
      DB_MIGRATION_USER: ""
      DB_MIGRATION_PASSWORD: ""
      CLOUDFLARE_API_TOKEN: ""
      MISTY_ACTIVEPIECES_MCP_URL: "${ACTIVEPIECES_PUBLIC_URL:?set ACTIVEPIECES_PUBLIC_URL}/mcp"
      MISTY_ACTIVEPIECES_PROXY_URL: http://activepieces-app:80
      MISTY_ACTIVEPIECES_BOOTSTRAP_SECRET: "${ACTIVEPIECES_JWT_SECRET:?set ACTIVEPIECES_JWT_SECRET}"
    ports:
      - "127.0.0.1:${MISTY_HOST_PORT:-8081}:8080"
    depends_on:
      database-permissions:
        condition: service_completed_successfully
        restart: true
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent http://127.0.0.1:8080/health >/dev/null"]
      interval: 3s
      timeout: 5s
      retries: 30
    networks: [edge, database]

volumes:
  pgdata:
  activepieces_pgdata:
  activepieces_redisdata:
  activepieces_cache:

networks:
  edge:
  database:
    internal: true
  activepieces-database:
    internal: true
