[extend]
useDefault = true

# Public compatibility fixtures use an explicitly documented test-only signing key.
[allowlist]
description = "Synthetic self-host compatibility tokens"
condition = "AND"
paths = ['''docs/migration/fixtures/self-host-proofs\.json$''']
regexTarget = "line"
regexes = ['''"(?:goToken|nodeToken)"''']
