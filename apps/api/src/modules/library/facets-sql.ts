// Existing Library facet SQL; item references use the audience-filtered CTE.
export const facetSql = {
  counts: "SELECT count(*) FILTER(WHERE i.lifecycle_state='ready' AND i.hidden=FALSE AND NOT EXISTS(SELECT 1 FROM space_library_asset_stack_members sm JOIN space_library_asset_stacks s ON s.id=sm.stack_id WHERE sm.space_library_item_id=i.id AND s.lifecycle_state='ready' AND s.cover_item_id<>i.id)),count(*) FILTER(WHERE i.lifecycle_state='ready' AND i.hidden=FALSE AND i.favorite=TRUE AND NOT EXISTS(SELECT 1 FROM space_library_asset_stack_members sm JOIN space_library_asset_stacks s ON s.id=sm.stack_id WHERE sm.space_library_item_id=i.id AND s.lifecycle_state='ready' AND s.cover_item_id<>i.id)),count(*) FILTER(WHERE i.lifecycle_state='ready' AND i.hidden=TRUE AND NOT EXISTS(SELECT 1 FROM space_library_asset_stack_members sm JOIN space_library_asset_stacks s ON s.id=sm.stack_id WHERE sm.space_library_item_id=i.id AND s.lifecycle_state='ready' AND s.cover_item_id<>i.id)),count(*) FILTER(WHERE i.lifecycle_state='trash') FROM accessible_items i WHERE i.space_id=$1",
  tags: "SELECT tag,tag,count(*) FROM accessible_items i CROSS JOIN LATERAL jsonb_array_elements_text(i.tags) tag WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND NOT EXISTS(SELECT 1 FROM space_library_asset_stack_members sm JOIN space_library_asset_stacks s ON s.id=sm.stack_id WHERE sm.space_library_item_id=i.id AND s.lifecycle_state='ready' AND s.cover_item_id<>i.id) AND ($2='' OR tag ILIKE $3) GROUP BY tag ORDER BY count(*) DESC,lower(tag) LIMIT 12",
  media_types: "SELECT kind,initcap(kind),count(*) FROM (SELECT CASE WHEN b.server_detected_mime_type LIKE 'image/%' THEN 'image' WHEN b.server_detected_mime_type LIKE 'video/%' THEN 'video' WHEN b.server_detected_mime_type LIKE 'audio/%' THEN 'audio' ELSE 'document' END kind FROM accessible_items i JOIN library_files f ON f.id=i.file_id JOIN library_blobs b ON b.id=f.blob_id WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND NOT EXISTS(SELECT 1 FROM space_library_asset_stack_members sm JOIN space_library_asset_stacks s ON s.id=sm.stack_id WHERE sm.space_library_item_id=i.id AND s.lifecycle_state='ready' AND s.cover_item_id<>i.id)) media WHERE ($2='' OR kind ILIKE $3) GROUP BY kind ORDER BY count(*) DESC,kind",
  years: "SELECT captured_year,captured_year,count(*) FROM (SELECT extract(year FROM COALESCE(i.date_override,f.intrinsic_capture_at,f.original_uploaded_at))::int::text captured_year FROM accessible_items i JOIN library_files f ON f.id=i.file_id WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND NOT EXISTS(SELECT 1 FROM space_library_asset_stack_members sm JOIN space_library_asset_stacks s ON s.id=sm.stack_id WHERE sm.space_library_item_id=i.id AND s.lifecycle_state='ready' AND s.cover_item_id<>i.id)) dates WHERE ($2='' OR captured_year ILIKE $3) GROUP BY captured_year ORDER BY captured_year DESC LIMIT 12",
  albums: "SELECT a.id,a.name,count(i.id) FROM space_albums a LEFT JOIN space_album_items ai ON ai.album_id=a.id LEFT JOIN accessible_items i ON i.id=ai.space_library_item_id AND i.lifecycle_state='ready' AND i.hidden=FALSE WHERE a.space_id=$1 AND ($2='' OR a.name ILIKE $3) GROUP BY a.id,a.name ORDER BY count(i.id) DESC,lower(a.name) LIMIT 12",
  utilities: "SELECT utility,label,item_count FROM (\n\t\t\t\tSELECT 'recently-viewed' utility,'Recently Viewed' label,count(*) item_count,1 sort_order FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND EXISTS(SELECT 1 FROM space_library_item_views v WHERE v.space_id=i.space_id AND v.space_library_item_id=i.id AND v.user_id=$2)\n\t\t\t\tUNION ALL SELECT 'recently-edited','Recently Edited',count(*),2 FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND i.current_edit_version_id IS NOT NULL\n\t\t\t\tUNION ALL SELECT 'recently-shared','Recently Shared',count(*),3 FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND (EXISTS(SELECT 1 FROM space_library_grants g WHERE g.source_space_id=i.space_id AND g.source_item_id=i.id AND g.state='active') OR EXISTS(SELECT 1 FROM space_message_library_references r WHERE r.space_id=i.space_id AND r.space_library_item_id=i.id))\n\t\t\t\tUNION ALL SELECT 'recently-saved','Recently Saved',count(*),4 FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND EXISTS(SELECT 1 FROM space_message_attachments a WHERE a.space_id=i.space_id AND a.promoted_item_id=i.id)\n\t\t\t\tUNION ALL SELECT 'recovered','Recovered',count(*),5 FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND EXISTS(SELECT 1 FROM space_library_audit_events e WHERE e.space_id=i.space_id AND e.target_kind='library_item' AND e.target_id=i.id AND e.action='library.item.restored' AND e.outcome='success')\n\t\t\t\tUNION ALL SELECT 'imports','Imports',count(*),6 FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND EXISTS(SELECT 1 FROM space_library_imports h WHERE h.destination_space_id=i.space_id AND h.destination_item_id=i.id AND h.state='ready')\n\t\t\t\tUNION ALL SELECT 'featured','Featured Photos',count(*),7 FROM accessible_items i JOIN library_files f ON f.id=i.file_id JOIN library_blobs b ON b.id=f.blob_id WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND NOT EXISTS(SELECT 1 FROM space_library_asset_stack_members sm JOIN space_library_asset_stacks s ON s.id=sm.stack_id WHERE sm.space_library_item_id=i.id AND s.lifecycle_state='ready' AND s.cover_item_id<>i.id) AND b.server_detected_mime_type LIKE 'image/%' AND (i.favorite OR EXISTS(SELECT 1 FROM library_derivatives d WHERE d.space_library_item_id=i.id AND d.lifecycle_state='ready' AND d.kind='ai_metadata' AND lower(d.metadata::text) ~ '(featured|aesthetic|best shot|high quality)'))\n\t\t\t\tUNION ALL SELECT 'screenshots','Screenshots',count(*),8 FROM accessible_items i JOIN library_files f ON f.id=i.file_id WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND (lower(f.original_filename) LIKE '%screenshot%' OR lower(f.intrinsic_metadata::text) LIKE '%screenshot%')\n\t\t\t\tUNION ALL SELECT 'documents','Documents',count(*),6 FROM accessible_items i JOIN library_files f ON f.id=i.file_id JOIN library_blobs b ON b.id=f.blob_id WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND b.server_detected_mime_type NOT LIKE 'image/%' AND b.server_detected_mime_type NOT LIKE 'video/%' AND b.server_detected_mime_type NOT LIKE 'audio/%'\n\t\t\t\tUNION ALL SELECT 'receipts','Receipts',count(*),7 FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND EXISTS(SELECT 1 FROM library_derivatives d WHERE d.space_library_item_id=i.id AND d.lifecycle_state='ready' AND d.kind='ai_metadata' AND lower(d.metadata::text) LIKE '%receipt%')\n\t\t\t\tUNION ALL SELECT 'handwriting','Handwriting',count(*),8 FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND EXISTS(SELECT 1 FROM library_derivatives d WHERE d.space_library_item_id=i.id AND d.lifecycle_state='ready' AND d.kind='ai_metadata' AND lower(d.metadata::text) LIKE '%handwrit%')\n\t\t\t\tUNION ALL SELECT 'illustrations','Illustrations',count(*),9 FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND EXISTS(SELECT 1 FROM library_derivatives d WHERE d.space_library_item_id=i.id AND d.lifecycle_state='ready' AND d.kind='ai_metadata' AND lower(d.metadata::text) LIKE '%illustration%')\n\t\t\t\tUNION ALL SELECT 'qr-codes','QR Codes',count(*),10 FROM accessible_items i WHERE i.space_id=$1 AND i.lifecycle_state='ready' AND i.hidden=FALSE AND EXISTS(SELECT 1 FROM library_derivatives d WHERE d.space_library_item_id=i.id AND d.lifecycle_state='ready' AND d.kind='ai_metadata' AND lower(d.metadata::text) LIKE '%qr%')\n\t\t\t) utility_rows WHERE item_count>0 AND ($3='' OR label ILIKE $4) ORDER BY sort_order",
} as const;
